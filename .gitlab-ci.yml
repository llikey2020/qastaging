workflow:
  rules:
    - if: $CI_COMMIT_TAG
    - if: '$CI_COMMIT_BRANCH'

default:
  tags: ["${GIT_RUNNER_TAG}"]

variables:
  GIT_RUNNER_TAG: "k8s-cluster-1"

.environment:
  environment: "${CI_ENVIRONMENT}"

include: staging.yml